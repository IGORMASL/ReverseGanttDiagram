# Быстрый старт для новых разработчиков

## 🎯 Что это за проект?

Веб-приложение для управления образовательными классами, проектами и командами. Позволяет:
- Создавать и управлять классами
- Создавать проекты внутри классов
- Формировать команды для работы над проектами
- Приглашать участников через ссылки-приглашения

## 👥 Роли пользователей

1. **Админ** - может всё
2. **Учитель** - управляет своими классами
3. **Студент** - видит только проекты, где состоит в команде

## 📍 Где что находится?

### Если нужно изменить отображение страницы:
→ `src/pages/` - здесь все страницы

### Если нужно изменить логику загрузки данных:
→ `src/hooks/` - здесь бизнес-логика
- `useAuth.ts` - авторизация
- `useClasses.ts` - список классов
- `useClassDetails.ts` - детали класса
- `useProject.ts` - детали проекта

### Если нужно изменить API запросы:
→ `src/api/` - здесь все запросы к бэкенду
- `classes.ts` - работа с классами
- `projects.ts` - работа с проектами
- `teams.ts` - работа с командами

### Если нужно изменить UI компонент:
→ `src/components/` - переиспользуемые компоненты

### Если нужно добавить проверку прав:
→ `src/utils/permissions.ts` - функции проверки прав

### Если нужно изменить обработку ошибок:
→ `src/utils/errorHandling.ts` - обработка ошибок

## 🔄 Типичный workflow

### Добавление новой функции

1. **Определить, где логика:**
   - Если это загрузка/обновление данных → добавить в хук
   - Если это UI состояние → добавить в страницу

2. **Добавить API функцию** (если нужно):
   ```typescript
   // src/api/yourEntity.ts
   export async function yourFunction(): Promise<YourType> {
     const res = await api.get("/your-endpoint");
     return res.data;
   }
   ```

3. **Использовать в хуке:**
   ```typescript
   // src/hooks/useYourEntity.ts
   const [data, setData] = useState<YourType | null>(null);
   
   useEffect(() => {
     yourFunction().then(setData);
   }, []);
   ```

4. **Использовать в странице:**
   ```typescript
   // src/pages/YourPage.tsx
   const { data } = useYourEntity();
   return <div>{data?.title}</div>;
   ```

## 🐛 Частые проблемы

### Проект не отображается у студента
- Проверьте, что студент добавлен в команду
- Проверьте, что студент является участником класса
- Студенту нужно обновить страницу после добавления в команду

### Ошибка 403 (Forbidden)
- Проверьте права пользователя через `canManageClass()`
- Убедитесь, что используется правильный API endpoint
- Для студентов используйте `getUserClassProjects`, не `getProjectById`

### Ошибка 400 (Bad Request)
- Проверьте формат данных в запросе
- Проверьте, что все обязательные поля заполнены
- Проверьте формат дат (должны быть ISO строки)

## 📚 Полезные ссылки

- **Полная документация:** `README.md`
- **Архитектура:** `ARCHITECTURE.md`
- **API документация:** смотрите комментарии в `src/api/*.ts`

## 💡 Советы

1. **Всегда проверяйте права** перед показом UI элементов
2. **Используйте хуки** для переиспользования логики
3. **Обрабатывайте ошибки** через `getErrorMessage()`
4. **Типизируйте всё** - TypeScript ваш друг
5. **Следуйте структуре** - не создавайте файлы где попало

## 🎨 Стиль кода

- Используйте функциональные компоненты
- Используйте хуки вместо классов
- Типизируйте props и state
- Используйте деструктуризацию
- Комментируйте сложную логику

## 🚀 Быстрые команды

```bash
# Запуск приложения
npm run dev

# Сборка для продакшена
npm run build

# Проверка типов
npm run type-check  # если есть такая команда
```

## 📞 Нужна помощь?

1. Проверьте `README.md` - там общая информация
2. Проверьте `ARCHITECTURE.md` - там детали архитектуры
3. Посмотрите похожий код в проекте
4. Проверьте комментарии в коде

