# Структура кода - Краткий справочник

## 📋 Быстрая навигация по коду

### Если нужно найти...

#### **Логику загрузки классов**
- `src/hooks/useClasses.ts` - хук для списка классов
- `src/api/classes.ts` - API функции для классов

#### **Логику работы с проектом**
- `src/hooks/useProject.ts` - хук для проекта и команд
- `src/api/projects.ts` - API функции для проектов
- `src/api/teams.ts` - API функции для команд

#### **Логику работы с классом**
- `src/hooks/useClassDetails.ts` - хук для деталей класса
- `src/pages/ClassDetailsPage.tsx` - страница класса

#### **Проверку прав доступа**
- `src/utils/permissions.ts` - функции проверки прав
- Используется в хуках для определения `canManage`

#### **Обработку ошибок**
- `src/utils/errorHandling.ts` - функции обработки ошибок
- Используется везде для единообразной обработки

#### **Типы данных**
- `src/types/index.ts` - общие типы
- `src/api/*.ts` - типы, специфичные для API

#### **Константы**
- `src/constants/index.ts` - все константы (роли, статусы, метки)

## 🔍 Где что лежит

```
src/
│
├── api/                    # ВСЕ запросы к бэкенду
│   ├── axios.ts           # Настройка Axios
│   ├── auth.ts            # Логин/регистрация
│   ├── classes.ts         # Классы
│   ├── projects.ts        # Проекты
│   ├── teams.ts           # Команды
│   ├── invite.ts          # Приглашения
│   └── users.ts           # Пользователи
│
├── hooks/                  # БИЗНЕС-ЛОГИКА (переиспользуемая)
│   ├── useAuth.ts         # Авторизация
│   ├── useClasses.ts      # Список классов
│   ├── useClassDetails.ts # Детали класса
│   └── useProject.ts      # Проект и команды
│
├── pages/                  # СТРАНИЦЫ (только UI)
│   ├── AuthPage.tsx
│   ├── ClassesPage.tsx
│   ├── ClassDetailsPage.tsx
│   └── ProjectPage.tsx
│
├── components/             # UI КОМПОНЕНТЫ
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── ClassCard.tsx
│   ├── HeaderClasses.tsx
│   ├── ProtectedRoute.tsx
│   └── *Modal.tsx         # Модальные окна
│
├── utils/                  # УТИЛИТЫ (чистые функции)
│   ├── errorHandling.ts   # Обработка ошибок
│   └── permissions.ts     # Проверка прав
│
├── types/                  # ТИПЫ (общие)
│   └── index.ts
│
└── constants/              # КОНСТАНТЫ
    └── index.ts
```

## 🎯 Принципы организации

### 1. Разделение ответственности

- **API** - только запросы к серверу
- **Hooks** - бизнес-логика и состояние
- **Pages** - композиция и UI
- **Components** - переиспользуемые UI элементы
- **Utils** - вспомогательные функции

### 2. Единая точка входа

- Все типы экспортируются через `index.ts`
- Все константы в одном месте
- Все утилиты в отдельных файлах

### 3. Переиспользование

- Логика вынесена в хуки
- UI компоненты переиспользуются
- Утилиты используются везде

## 📝 Примеры использования

### Добавление нового API endpoint

```typescript
// src/api/yourEntity.ts
export async function getYourData(id: string): Promise<YourType> {
  const res = await api.get(`/your-endpoint/${id}`);
  return res.data;
}
```

### Создание нового хука

```typescript
// src/hooks/useYourEntity.ts
export function useYourEntity(id: string) {
  const [data, setData] = useState<YourType | null>(null);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    getYourData(id).then(setData).finally(() => setLoading(false));
  }, [id]);
  
  return { data, loading };
}
```

### Использование в странице

```typescript
// src/pages/YourPage.tsx
export default function YourPage() {
  const { id } = useParams();
  const { data, loading } = useYourEntity(id!);
  
  if (loading) return <Loading />;
  return <div>{data?.title}</div>;
}
```

## 🔗 Зависимости между слоями

```
Pages → Hooks → API → Backend
  ↓       ↓
Components Utils
```

- **Pages** используют **Hooks** и **Components**
- **Hooks** используют **API** и **Utils**
- **API** использует **Utils** (для обработки ошибок)
- **Components** независимы (получают данные через props)

## ✅ Чеклист для нового разработчика

- [ ] Прочитал `README.md` - общее понимание
- [ ] Прочитал `QUICK_START.md` - быстрый старт
- [ ] Прочитал `ARCHITECTURE.md` - архитектура
- [ ] Изучил структуру папок
- [ ] Понял систему ролей
- [ ] Понял систему прав доступа
- [ ] Изучил примеры хуков
- [ ] Изучил примеры страниц
- [ ] Готов к разработке! 🚀

