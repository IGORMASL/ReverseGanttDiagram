# GanttChart Web Application

Веб-приложение для управления классами, проектами и командами с диаграммами Ганта.

## 📁 Структура проекта

```
src/
├── api/              # API клиенты и типы для работы с бэкендом
│   ├── axios.ts      # Настройка Axios с базовым URL и interceptors
│   ├── auth.ts       # Аутентификация (логин, регистрация)
│   ├── classes.ts    # API для работы с классами
│   ├── projects.ts   # API для работы с проектами
│   ├── teams.ts      # API для работы с командами
│   ├── invite.ts     # API для работы с приглашениями
│   └── users.ts      # API для работы с пользователями
│
├── components/       # Переиспользуемые UI компоненты
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── ClassCard.tsx
│   ├── HeaderClasses.tsx
│   ├── ProtectedRoute.tsx
│   ├── CreateClassModal.tsx
│   ├── CreateInviteModal.tsx
│   ├── JoinClassModal.tsx
│   └── ProjectModal.tsx
│
├── pages/            # Страницы приложения
│   ├── AuthPage.tsx          # Страница входа/регистрации
│   ├── ClassesPage.tsx       # Список классов
│   ├── ClassDetailsPage.tsx  # Детали класса (проекты, участники)
│   └── ProjectPage.tsx       # Детали проекта (команды, задачи)
│
├── hooks/            # Кастомные React хуки
│   ├── useAuth.ts           # Хук для работы с аутентификацией
│   ├── useClasses.ts        # Хук для работы со списком классов
│   ├── useClassDetails.ts   # Хук для работы с деталями класса
│   └── useProject.ts        # Хук для работы с проектом
│
├── types/            # TypeScript типы
│   └── index.ts      # Централизованные типы приложения
│
├── utils/            # Утилиты и вспомогательные функции
│   ├── errorHandling.ts  # Обработка ошибок
│   └── permissions.ts     # Проверка прав доступа
│
├── constants/        # Константы приложения
│   └── index.ts      # Роли, статусы, метки
│
├── App.tsx           # Главный компонент с роутингом
└── main.tsx          # Точка входа приложения
```

## 🎯 Основные концепции

### Роли пользователей

1. **Системный админ (role = 1)**
   - Полный доступ ко всем классам
   - Может создавать, редактировать и удалять классы
   - Может управлять всеми проектами и командами

2. **Учитель (roleInClass = 1)**
   - Может управлять классом, в котором является учителем
   - Может создавать/редактировать/удалять проекты в классе
   - Может создавать команды и добавлять участников
   - Может создавать приглашения

3. **Студент (roleInClass = 0)**
   - Видит только проекты, в командах которых состоит
   - Может просматривать свою команду
   - Не может редактировать проекты или управлять командами

### Система прав доступа

Права определяются через функцию `canManageClass()` в `src/utils/permissions.ts`:
- Системный админ всегда может управлять
- Учитель может управлять только в своих классах
- Студент не может управлять

## 🔄 Потоки данных

### Загрузка классов

```
ClassesPage
  └─> useAuth() → получает профиль пользователя
  └─> useClasses(user) → загружает классы в зависимости от роли
      ├─ Админ: getAllClasses()
      └─ Пользователь: getUserClasses()
```

### Загрузка деталей класса

```
ClassDetailsPage
  └─> useAuth() → получает профиль
  └─> useClassDetails({ classId, user }) → загружает:
      ├─ getClassDetails(classId)
      ├─ getClassMembers(classId)
      └─ getClassProjects() или getUserClassProjects() (в зависимости от роли)
```

### Загрузка проекта

```
ProjectPage
  └─> useAuth() → получает профиль
  └─> useProject({ projectId, classId, user }) → загружает:
      ├─ Админ/Учитель: getProjectById(projectId)
      ├─ Студент: getUserClassProjects() → find(projectId)
      ├─ getClassMembers(classId) → для проверки прав
      └─ getProjectTeams(projectId) → команды проекта (только для админа/учителя)
```

## 📦 API структура

### Классы (`/api/class`)
- `GET /api/class` - все классы (админ)
- `GET /api/class/user` - классы пользователя
- `GET /api/class/{id}` - детали класса
- `GET /api/class/members/{classId}` - участники класса
- `POST /api/class` - создание класса
- `PUT /api/class?classId=...` - обновление класса
- `DELETE /api/class?id=...` - удаление класса

### Проекты (`/api/project`)
- `GET /api/project/class/{classId}` - проекты класса (админ/учитель)
- `GET /api/project/class/{classId}/user` - проекты класса для студента
- `GET /api/project/{projectId}` - детали проекта (админ/учитель)
- `POST /api/project` - создание проекта
- `PUT /api/project?projectId=...` - обновление проекта
- `DELETE /api/project?projectId=...` - удаление проекта

### Команды (`/api/team`)
- `GET /api/team/project?projectId=...` - команды проекта (с участниками)
- `POST /api/team` - создание команды
- `PUT /api/team/members?teamId=...&userId=...` - добавление участника

### Приглашения (`/api/invite`)
- `POST /api/invite/create?classId=...` - создание приглашения
- `POST /api/invite/{inviteId}` - использование приглашения

## 🎨 Компоненты

### Модальные окна

- **CreateClassModal** - создание/редактирование класса
- **CreateInviteModal** - создание ссылки-приглашения
- **JoinClassModal** - присоединение к классу по коду
- **ProjectModal** - создание/редактирование проекта

### Переиспользуемые компоненты

- **Button** - кнопка с вариантами стилей
- **Input** - текстовое поле ввода
- **ClassCard** - карточка класса
- **HeaderClasses** - шапка страницы с навигацией
- **ProtectedRoute** - защищенный роут (требует авторизации)

## 🔧 Кастомные хуки

### `useAuth()`
Загружает профиль текущего пользователя.

**Возвращает:**
```typescript
{
  user: CurrentUser | null;
  loading: boolean;
  error: string | null;
}
```

### `useClasses(user)`
Управляет списком классов пользователя.

**Возвращает:**
```typescript
{
  classes: UserClass[];
  loading: boolean;
  error: string | null;
  handleDeleteClass: (classId: string) => Promise<boolean>;
  handleCreateClass: (data: ClassCreateDto) => Promise<boolean>;
  handleUpdateClass: (classId: string, data: {...}) => Promise<boolean>;
}
```

### `useClassDetails({ classId, user })`
Управляет деталями класса, проектами и участниками.

**Возвращает:**
```typescript
{
  details: ClassDetails | null;
  projects: Project[];
  loading: boolean;
  error: string | null;
  canManage: boolean;
  handleDeleteClass: () => Promise<boolean>;
  handleUpdateClass: (data: {...}) => Promise<boolean>;
  handleCreateProject: (data: {...}) => Promise<boolean>;
  handleUpdateProject: (projectId: string, data: {...}) => Promise<boolean>;
  handleDeleteProject: (projectId: string) => Promise<boolean>;
}
```

### `useProject({ projectId, classId, user })`
Управляет проектом и командами.

**Возвращает:**
```typescript
{
  project: Project | null;
  teams: Team[];
  teamsWithMembers: Map<string, TeamWithMembers>;
  canManage: boolean;
  classMembers: ClassMember[];
  myTeam: TeamWithMembers | null;
  loading: boolean;
  error: string | null;
  handleCreateTeam: (name: string) => Promise<boolean>;
  handleAddMemberToTeam: (teamId: string, userId: string) => Promise<boolean>;
  handleUpdateProject: (data: {...}) => Promise<boolean>;
  handleDeleteProject: () => Promise<boolean>;
  refreshTeams: () => Promise<void>;
}
```

## 🛠️ Утилиты

### `errorHandling.ts`
- `getErrorMessage(error)` - извлекает сообщение об ошибке
- `isForbiddenError(error)` - проверяет 403 ошибку
- `isValidationError(error)` - проверяет 400 ошибку

### `permissions.ts`
- `isSystemAdmin(user)` - проверяет системного админа
- `isTeacherInClass(email, members)` - проверяет учителя в классе
- `canManageClass(user, members)` - проверяет права на управление

## 🚀 Начало работы

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Настройка API:**
   - Убедитесь, что бэкенд запущен на `http://localhost:5050`
   - Или измените `VITE_API_URL` в `.env`

3. **Запуск приложения:**
   ```bash
   npm run dev
   ```

## 📝 Важные замечания

1. **Для студентов:**
   - Проекты отображаются только если студент добавлен в команду
   - После добавления в команду нужно обновить страницу класса
   - Для загрузки проекта используется `getUserClassProjects`, а не `getProjectById`

2. **Права доступа:**
   - Проверка прав происходит на фронтенде через `canManage`
   - Бэкенд также проверяет права и может вернуть 403

3. **Обработка ошибок:**
   - Все ошибки обрабатываются через `getErrorMessage()`
   - Пользователю показываются понятные сообщения

## 🔮 Будущие улучшения

- [ ] Реализация списка задач для студентов
- [ ] Удаление участников из класса
- [ ] Удаление участников из команды
- [ ] Редактирование команды
- [ ] Диаграмма Ганта для проектов
- [ ] Уведомления о новых проектах/командах

